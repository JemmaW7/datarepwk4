//further modularizing app with header component.
//added a bootstrap class to align the text to the center of the app

const Header = () => {
    return <h1 className="text-center">My Header in another component</h1>;
  };
  
  export default Header;